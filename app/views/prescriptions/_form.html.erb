<%= form_for(@prescription) do |f| %>
  <% if @prescription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prescription.errors.count, "error") %> prohibited this prescription from being saved:</h2>

      <ul>
      <% @prescription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prescription_details %><br>
    <%= f.text_area :prescription_details %>
  </div>
  <div class="field">
    <%= f.label :prescription_date %><br>
    <%= f.date_select :prescription_date %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :patient_id %><br>
    <%= f.collection_select:patient_id,Patient.all, :id, :email %>
  </div>
  <div class="field">
    <%= f.label :doctor_id %><br>
    <%= f.collection_select:doctor_id,Doctor.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
